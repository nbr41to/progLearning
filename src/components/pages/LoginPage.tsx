import { VFC } from "react";

type LoginPageProps = {};

export const LoginPage: VFC<LoginPageProps> = () => {
  return (
    <div>
      <h1>Login</h1>
      <div>Google Login</div>
    </div>
  );
};
